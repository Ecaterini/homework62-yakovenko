doctype html
html
  head
    title Users
    link(rel="stylesheet", href="/css/style.css")
  body
    h1 Users list
    ul
      each user in users
        li
          a(href=`/users/${user.id}`) #{user.username}